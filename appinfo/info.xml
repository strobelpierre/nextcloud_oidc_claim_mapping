<?xml version="1.0"?>
<!--
  - SPDX-FileCopyrightText: 2026 OIDC Claim Mapping Contributors
  - SPDX-License-Identifier: AGPL-3.0-or-later
-->
<info xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://apps.nextcloud.com/schema/apps/info.xsd">
    <id>oidc_claim_mapping</id>
    <name>OIDC Claim Mapping</name>
    <summary>Map OIDC token claims to Nextcloud user attributes with a visual rule editor</summary>
    <description><![CDATA[
Map OIDC token claims to scalar Nextcloud user attributes (display name, email, quota,
country, language, etc.) using configurable rules. Hook into the user_oidc app's
`AttributeMappedEvent` so any rule can override the value computed natively by user_oidc.

**Admin UI** — visual rule editor with target dropdown, raw JSON editor, and claim simulator.
**Multiple rule types** — direct, prefix, map, conditional, template (full Mustache).
**Per-provider scope** — bind a rule to one OIDC provider via the `iss` claim, or `*` to apply globally.
**REST API** — manage rules programmatically via OCS endpoints.
**OCC commands** — list, set, and test rules from the CLI.
**Dark mode** — full support for Nextcloud dark and light themes.

Works with any identity provider through the user_oidc app. Requires user_oidc to be
installed and enabled.

V1 targets only scalar user attributes (no groups). The group mapping path remains in
the companion app oidc_groups_mapping. Future major versions may merge both surfaces.
    ]]></description>
    <version>0.1.0-dev</version>
    <licence>agpl</licence>
    <author>Pierre Strobel</author>
    <namespace>OidcClaimMapping</namespace>
    <category>integration</category>
    <website>https://github.com/strobelpierre/nextcloud_oidc_claim_mapping</website>
    <bugs>https://github.com/strobelpierre/nextcloud_oidc_claim_mapping/issues</bugs>
    <repository type="git">https://github.com/strobelpierre/nextcloud_oidc_claim_mapping</repository>
    <dependencies>
        <nextcloud min-version="29" max-version="32"/>
        <php min-version="8.1"/>
    </dependencies>
    <types>
        <authentication/>
    </types>
    <settings>
        <admin>OCA\OidcClaimMapping\Settings\AdminSettings</admin>
        <admin-section>OCA\OidcClaimMapping\Settings\Section</admin-section>
    </settings>
    <commands>
        <command>OCA\OidcClaimMapping\Command\ListRules</command>
        <command>OCA\OidcClaimMapping\Command\SetRules</command>
        <command>OCA\OidcClaimMapping\Command\TestMapping</command>
    </commands>
</info>
