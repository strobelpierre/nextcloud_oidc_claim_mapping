{
    "name": "nextcloud/oidc_claim_mapping",
    "description": "Map OIDC token claims to scalar Nextcloud user attributes via the user_oidc AttributeMappedEvent",
    "license": "AGPL-3.0-or-later",
    "autoload": {
        "psr-4": {
            "OCA\\OidcClaimMapping\\": "lib/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "OCA\\OidcClaimMapping\\Tests\\": "tests/"
        }
    },
    "config": {
        "optimize-autoloader": true,
        "allow-plugins": {
            "bamarni/composer-bin-plugin": true
        }
    },
    "scripts": {
        "test:unit": "phpunit -c tests/phpunit.xml",
        "cs:check": "php-cs-fixer fix --dry-run --diff",
        "cs:fix": "php-cs-fixer fix",
        "psalm": "psalm --no-cache",
        "lint": "find . -name '*.php' ! -path './vendor/*' ! -path './vendor-bin/*' ! -path './tests/stubs/*' -exec php -l {} \\;"
    },
    "require": {
        "php": ">=8.1",
        "mustache/mustache": "^2.14"
    },
    "require-dev": {
        "nextcloud/ocp": "dev-stable29",
        "phpunit/phpunit": "^10 || ^11",
        "vimeo/psalm": "^5 || ^6",
        "bamarni/composer-bin-plugin": "^1.9"
    }
}
